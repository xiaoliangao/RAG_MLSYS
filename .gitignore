# ===================================================================
# Python Standard Ignores
# ===================================================================
# Compiled Python files and cache
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
venv/
env/
*.venv

# Distribution / build artifacts
.eggs/
*.egg-info/
dist/
build/

# Environment variables (NEVER commit these)
.env
.env.*

# ===================================================================
# OS / IDE Specific Ignores
# ===================================================================
# macOS
.DS_Store

# Windows
Thumbs.db

# VSCode
.vscode/

# PyCharm
.idea/

# ===================================================================
# Project-Specific Ignores
# ===================================================================

# 1. Models (Large downloaded files)
# Ignore model files but keep the directory structure
models/*.bin
models/*.gguf
models/*.safetensors
models/*.pth
models/*.pt
models/*.h5
models/*.onnx

# 2. Vector Databases (Generated data)
# Ignore the database files but keep the directory
vector_db/*.db
vector_db/*.sqlite
vector_db/*.index
vector_db/session_*

# 3. Processed Data (Generated artifacts)
# Ignore processed files but keep the directory
processed_chunks/*.pkl
processed_chunks/*.json

# 4. User Uploads (Volatile data)
# Ignore uploaded PDFs
temp_uploads/*.pdf

# 5. Task/Job Status (Logs)
# Ignore status files but keep the directory
task_status/*.json

# 6. Feedback Database (User-generated data)
# Ignore feedback files but keep the directory
feedback_db/*.json
feedback_db/*.db

# 7. Source Data (Large binary files)
# Ignore PDFs in knowledge base but keep the directory
knowledge_base/*.pdf

# Keep directory structure with .gitkeep files
!.gitkeep